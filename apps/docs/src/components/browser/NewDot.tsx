/** Small accent dot marking new items (components, sidebar links). */
export function NewDot() {
  return (
    <span
      role="img"
      aria-label="New"
      className="size-1.5 rounded-full bg-accent"
    />
  );
}
