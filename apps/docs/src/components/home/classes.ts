/** Shared terminal-label recipe — mono microcopy with wide tracking,
 * used by pane headers, receipts, and status lines across the homepage
 * sections. Compose with a text color and layout classes at the call
 * site. */
export const TERMINAL_LABEL =
  "font-mono text-[10px] tracking-[0.2em] uppercase";
