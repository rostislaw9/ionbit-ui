/**
 * Theme preset type definitions — re-exported from the canonical
 * location in @ionbit-ui/tokens so docs, the CLI, and the registry
 * build share one source.
 */
export type * from "@ionbit-ui/tokens/theme-types";
