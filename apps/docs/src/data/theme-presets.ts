/**
 * Theme preset barrel — re-exports types and preset data.
 */

export type {
  ShadowIntensity,
  ThemeColors,
  ThemeRadius,
  ThemeSettings,
  NumericThemeSettingKey,
  ThemePreset,
} from "./theme-types";

export { THEME_PRESETS, DEFAULT_THEME_ID } from "./theme-presets-data";
